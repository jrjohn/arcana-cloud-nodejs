FROM node:22-alpine
WORKDIR /app
COPY package.json package-lock.json ./
RUN npm ci --legacy-peer-deps
RUN npm install @vitest/coverage-v8@^2.1.0
COPY prisma ./prisma/
RUN npx prisma generate
COPY tsconfig.json vitest.config.ts ./
COPY src ./src/
COPY tests ./tests/
